{
  "name": "wagetheft-frontend",
  "version": "1.0.0",
  "description": "The user-facing website for helping workers recover wages",
  "author": "Code For America",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "staging": "git checkout staging && git merge master && git push && git checkout master",
    "production": "git checkout production && git merge master && git push && git checkout master"
  },
  "dependencies": {
    "nuxt": "^2.0.0",
    "@nuxtjs/axios": "^5.3.6",
    "@nuxtjs/pwa": "^3.0.0-0"
  },
  "devDependencies": {
    "@nuxtjs/eslint-config": "^1.0.1",
    "@nuxtjs/eslint-module": "^1.0.0",
    "babel-eslint": "^10.0.1",
    "eslint": "^6.1.0",
    "eslint-plugin-nuxt": ">=0.4.2",
    "node-sass": "7.0.0",
    "nuxt-i18n": "6.4.1",
    "sass-loader": "8.0.0",
    "v-click-outside": "3.0.0",
    "vue-shortkey": "3.1.7"
  }
}
